services:
  app:
    volumes:
      # База знаний монтируется СТРОГО как RO (Read-Only)
      - /var/syncthing/vault:/app/vault:ro

      # Песочница агента (единственная RW зона для кэша и памяти)
      - "./data:/app/data:rw"
